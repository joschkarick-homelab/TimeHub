# --- App ---
APP_NAME=TimeHub
APP_ENV=production
LOG_LEVEL=info

# --- Security ---
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=change-me-please-use-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# --- Database ---
# Postgres password used by both the db service and the app's DATABASE_URL
# (docker-compose.prod.yml builds the URL from it; the dev compose ignores it).
POSTGRES_PASSWORD=changeme-postgres

# PostgreSQL (production / docker-compose default) — only override if you point
# the app at an external DB. The prod compose file ignores this and builds the
# URL from POSTGRES_PASSWORD above.
DATABASE_URL=postgresql+psycopg://timehub:changeme-postgres@db:5432/timehub
# SQLite (local dev fallback)
# DATABASE_URL=sqlite:///./data/timehub.sqlite

# --- Deployment ---
# Host port the reverse proxy / Proxmox forwards to the app container.
APP_PORT=8000
# Image tag to pull in docker-compose.prod.yml (latest, or a sha-... pin)
TIMEHUB_TAG=latest

# --- Initial admin (created on first start if no users exist) ---
INITIAL_ADMIN_EMAIL=admin@example.com
INITIAL_ADMIN_PASSWORD=changeme
INITIAL_ADMIN_NAME=Admin

# --- CORS (comma-separated origins, or * for all) ---
CORS_ORIGINS=*

# --- AI-assisted CSV mapping (optional) ---
# Leave empty to disable AI suggestions; manual mapping still works.
ANTHROPIC_API_KEY=
AI_MAPPING_MODEL=claude-sonnet-4-6
