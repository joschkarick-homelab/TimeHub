fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic[email]==2.10.3
pydantic-settings==2.7.0
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
python-multipart==0.0.20
PyJWT==2.10.1
cryptography>=42.0.0  # JWT signing + encrypting secrets at rest (Fernet)
passlib==1.7.4
bcrypt==4.0.1
jinja2==3.1.5
itsdangerous==2.2.0
httpx==0.28.1
anthropic==0.40.0
# MCP server mounted at /mcp. Pinned to the last release compatible with this
# stack: mcp>=1.13 requires pydantic>=2.11 (we pin 2.10.3), and newer releases
# also bump starlette past fastapi 0.115's starlette<0.42 bound.
mcp==1.12.4
